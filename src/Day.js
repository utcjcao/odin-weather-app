class Day {
  constructor(datetime, tempmax, tempmin, conditions) {
    this.datetime = datetime;
    this.tempmax = tempmax;
    this.tempmin = tempmin;
    this.conditions = conditions;
  }
}

export default Day;
